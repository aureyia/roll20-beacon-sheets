<div class="compact-header">
  {{#if characterName}}
    <div class="compact-header__character-name">{{characterName}}</div>
  {{/if}}
  {{#if @partial-block}}
    <div class="compact-header__group">
      {{> @partial-block }}
      <div class="compact-header__sub-group">
        <div class="compact-header__title">{{title}}</div>
      </div>
    </div>
  {{else}}
    <div class="compact-header__sub-group">
      <div class="compact-header__title">{{title}}</div>
    </div>
  {{/if}}
</div>
